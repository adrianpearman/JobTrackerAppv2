const initialState = {
    userId: '5cfbe3c271d5ee293ab8a643'
}

export const userReducer = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state
        }
    }
}