import ACTIONS from "../actions/types";

const initialState = {
  isLoggedIn: true
};

export const uiReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
